const media = '@media only screen and (min-width:';

const size = {
  md: '992px',
  lg: '1432px',
};

const breakpoints = {
  md: `${media} ${size.md})`,
  lg: `${media} ${size.lg})`,
};

export default breakpoints;
